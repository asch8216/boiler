import axios from 'axios';
const initialState = {};

//action types

//action creators

//thunks

//reducer

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
